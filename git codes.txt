
git init

git statuse


git config --global user.name "SHIHAB"

git config --global user.email "sshihabb007@gmail.com"

git config --list


git add .

git commit (press I then type and pres ESC)

git commit -m "type here"

git log

git log --onelene

git checkout 'code'

git checkout main

git push -u origin main

git diff

Git remote add origin 'Link'

git push -u origin main

git clone URL

git remove file_name

got reset head file_name

git fetch 

git pull

git branch idbranch

git checkout idbranch

git checkout -b "branch name"

git merge idbranch


